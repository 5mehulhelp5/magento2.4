<?xml version="1.0"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:module:Magento_Config:etc/system_file.xsd">

    <system>
        <!-- Add new tab -->
        <tab id="stoyanov_restaurants" translate="label" sortOrder="100">
            <label>Restaurants</label>
        </tab>

        <!-- Add new section -->
        <section id="restaurants_settings"
                 translate="label"
                 sortOrder="10"
                 showInDefault="1"
                 showInWebsite="1"
                 showInStore="1">
            <label>Restaurants Settings</label>
            <tab>stoyanov_restaurants</tab>
            <resource>Stoyanov_Restaurants::config</resource>

            <!-- Add group -->
            <group id="general"
                   translate="label"
                   sortOrder="10"
                   showInDefault="1"
                   showInWebsite="1"
                   showInStore="1">
                <label>General Settings</label>

                <!-- Add field -->
                <field id="page_size"
                       translate="label"
                       type="text"
                       sortOrder="10"
                       showInDefault="1"
                       showInWebsite="1"
                       showInStore="1">
                    <label>Shown restaurants per page</label>
                    <comment>Set restaurants per page number</comment>
                    <validate>validate-digits</validate>
                </field>
            </group>
        </section>
    </system>
</config>
